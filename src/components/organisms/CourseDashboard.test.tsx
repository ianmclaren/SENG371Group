import { render } from "@testing-library/react"
import CourseDashboard from "./CourseDashboard"

//
describe(CourseDashboard, () => {
    it("should allow user to click in to there course page", () => {
        expect(true).toBe(true)
    });
});

describe(CourseDashboard, () => {
    it("should allow user to click in to there account page", () => {
        expect(true).toBe(true)
    });
});

describe(CourseDashboard, () => {
    it("should allow user to see notifications", () => {
        expect(true).toBe(true)
    });
});

describe(CourseDashboard, () => {
    it("should allow user to select current term", () => {
        expect(true).toBe(true)
    });
});
